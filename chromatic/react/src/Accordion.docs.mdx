import { Canvas, Meta, Story, Subtitle, Title } from '@storybook/addon-docs/blocks';
import * as AccordionStories from './Accordion.stories';

<Meta of={AccordionStories} title="React/Accordion Docs" />

<Title>React Accordion</Title>
<Subtitle>Accessible disclosure content for product, support, and documentation flows.</Subtitle>

The React package exposes a compound Accordion API that keeps one element per part, supports controlled and uncontrolled state, and preserves the WAI-ARIA accordion relationships for triggers and panels.

## Purpose

Use Accordion when people need to progressively reveal supporting detail without losing the structure of the surrounding page. Good examples include rollout plans, FAQ groupings, pricing clarifications, policy detail, and implementation handoff notes.

## API Summary

<table>
  <thead>
    <tr>
      <th>Part</th>
      <th>Prop / event</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>Accordion</code></td>
      <td><code>value</code></td>
      <td>Controls the currently open item in single mode or the open item list in multiple mode.</td>
    </tr>
    <tr>
      <td><code>Accordion</code></td>
      <td><code>defaultValue</code></td>
      <td>Sets the initial open item or items for uncontrolled usage.</td>
    </tr>
    <tr>
      <td><code>Accordion</code></td>
      <td><code>onValueChange</code></td>
      <td>Receives the next open value after click or keyboard interaction.</td>
    </tr>
    <tr>
      <td><code>Accordion</code></td>
      <td><code>multiple</code></td>
      <td>Allows more than one item to stay open at the same time.</td>
    </tr>
    <tr>
      <td><code>Accordion</code></td>
      <td><code>collapsible</code></td>
      <td>Allows the last open item to collapse in single mode.</td>
    </tr>
    <tr>
      <td><code>Accordion</code></td>
      <td><code>disabled</code></td>
      <td>Disables the full control and prevents interaction for every item.</td>
    </tr>
    <tr>
      <td><code>Accordion</code></td>
      <td><code>loopFocus</code></td>
      <td>Keeps roving keyboard navigation looping from the last trigger back to the first and vice versa.</td>
    </tr>
    <tr>
      <td><code>Accordion</code></td>
      <td><code>hiddenUntilFound</code></td>
      <td>Lets browser find-in-page reveal closed panels when supported by the platform.</td>
    </tr>
    <tr>
      <td><code>Accordion</code></td>
      <td><code>size</code></td>
      <td>Selects the density tokens that drive spacing, padding, and trigger height.</td>
    </tr>
    <tr>
      <td><code>AccordionItem</code></td>
      <td><code>value</code></td>
      <td>Provides the stable item key used for open state, IDs, and programmatic control.</td>
    </tr>
    <tr>
      <td><code>AccordionItem</code></td>
      <td><code>disabled</code></td>
      <td>Disables a single item without affecting the rest of the Accordion.</td>
    </tr>
    <tr>
      <td><code>AccordionItem</code></td>
      <td><code>onOpenChange</code></td>
      <td>Reports when that item opens or closes.</td>
    </tr>
    <tr>
      <td><code>AccordionHeader</code></td>
      <td>Native heading attributes</td>
      <td>Lets consuming teams choose heading semantics without adding wrapper nodes.</td>
    </tr>
    <tr>
      <td><code>AccordionTrigger</code></td>
      <td>Native button attributes</td>
      <td>Supports framework-native button props while maintaining the Accordion ARIA contract.</td>
    </tr>
    <tr>
      <td><code>AccordionPanel</code></td>
      <td><code>hiddenUntilFound</code></td>
      <td>Keeps closed content discoverable for browser find-in-page when requested.</td>
    </tr>
    <tr>
      <td><code>AccordionPanel</code></td>
      <td><code>keepMounted</code></td>
      <td>Keeps closed panel content mounted instead of unmounting it when the item collapses.</td>
    </tr>
    <tr>
      <td><code>AccordionPanel</code></td>
      <td>Native <code>div</code> attributes</td>
      <td>Accepts normal panel host attributes while preserving the Accordion ARIA relationship.</td>
    </tr>
  </tbody>
</table>

## Usage Guidance

- Prefer `button` semantics for the trigger content and keep labels concise enough to scan quickly.
- Use controlled mode when the surrounding page needs to open sections programmatically.
- Use `multiple` only when comparing sections side by side is genuinely useful.
- Leave `keepMounted` at its default `false` unless hidden content must stay searchable, measurable, or available for animation orchestration.
- Provide a stable root `id` when multiple accordions can appear on the same view.

## Accessibility Guidance

- Keyboard users can move between triggers with `ArrowUp`, `ArrowDown`, `Home`, and `End`.
- Triggers expose `aria-expanded`, `aria-controls`, and disabled state consistently.
- Panels expose `role="region"` and `aria-labelledby` so screen readers can connect each panel to its trigger.
- Pointer and keyboard activation keep focus on the trigger that changed state so the visible focus indicator stays anchored to the active control.
- Keep trigger text meaningful out of context so screen reader rotor/navigation lists stay useful.

## Styling Hooks

The shared stylesheet exposes CSS variables for focus ring, spacing, and motion. Import `@accretion-ui/react/styles.css` in consuming applications before rendering the Accordion.

<table>
  <thead>
    <tr>
      <th>CSS variable</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>--ac-accordion-root-border-color</code></td>
      <td>Sets the default wrapper outline around the grouped Accordion items.</td>
    </tr>
    <tr>
      <td><code>--ac-accordion-root-border-color-open</code></td>
      <td>Defines the wrapper highlight shown when one or more items are expanded.</td>
    </tr>
    <tr>
      <td><code>--ac-accordion-root-padding</code></td>
      <td>Controls the inset between the wrapper outline and the item stack for each size variant.</td>
    </tr>
    <tr>
      <td><code>--ac-accordion-trigger-background-open</code></td>
      <td>Styles the trigger surface when its panel is expanded.</td>
    </tr>
    <tr>
      <td><code>--ac-accordion-trigger-min-height</code></td>
      <td>Sets the minimum interactive height for compact, comfortable, and spacious variants.</td>
    </tr>
    <tr>
      <td><code>--ac-accordion-panel-padding-inline</code></td>
      <td>Controls the readable left and right content padding inside panels.</td>
    </tr>
    <tr>
      <td><code>--ac-accordion-panel-padding-block-start</code></td>
      <td>Controls the top padding inside panels. The default matches the bottom padding for balanced spacing.</td>
    </tr>
    <tr>
      <td><code>--ac-accordion-focus-ring-color</code></td>
      <td>Defines the focus outline color used on triggers after pointer or keyboard activation.</td>
    </tr>
    <tr>
      <td><code>--ac-accordion-focus-ring-offset</code></td>
      <td>Sets how far the trigger focus ring is inset from the trigger edge.</td>
    </tr>
  </tbody>
</table>

## Examples

<Canvas>
  <Story of={AccordionStories.Default} />
</Canvas>

<Canvas>
  <Story of={AccordionStories.ControlledSingle} />
</Canvas>
